//number, string

// Let's declare a number variable that is a constant and give it a value. Change that to a non constatnt and see the difference.
// Try to give a string as a value

// We explore concepts such as Type Assignment and Type Inference.

const lang = 'TypeScript';
const version = 5.2;

// boolean

const isValid: boolean = false; //Note that if we put a falsy value such as null it will throw an error. only true and false are allowed.
